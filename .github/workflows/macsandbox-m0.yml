name: MacSandbox M0

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  qualify-m0:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - name: Qualify M0
        run: python3 tools/check_m0.py
